---
interface Props {
  lang: 'zh' | 'en';
}

const { lang } = Astro.props;
const label = lang === 'zh' ? '中文' : 'EN';
---

<span
  class="font-sans text-[10px] uppercase tracking-[0.25em] text-neutral-500 border border-neutral-300 px-2 py-0.5"
>
  {label}
</span>
